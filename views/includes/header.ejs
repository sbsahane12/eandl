<nav class="navbar navbar-expand-lg navbar-custom sticky-top" style="background-color: #c471f5;">
    <div class="container-fluid">
        <img class="navbar-brand" src="/image/logo.png" alt="not found" style="width: 150px; height: 50px;">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fa-solid fa-bars" style="color: white;"></i>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                   <a class="nav-link active" aria-current="page" href="/" style="color: white;">Home</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                <% if (currentUser) { %>
                    <% if (currentUser.role === "admin") { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/profile" style="color: white;">Admin Dashboard</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/user/profile" style="color: white;">User Profile</a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a href="/user/logout" class="btn btn-danger">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/user/login" style="color: white;">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/user/signup" style="color: white;">Sign Up</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
